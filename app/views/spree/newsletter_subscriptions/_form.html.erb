<%= form_tag newsletter_subscriptions_path, :remote => true do |f| %>
  <%= text_field_tag :email, nil, :class => "email", :id => "subscribe_email", :placeholder => t('newsletter.enter_email') %>
  <%= submit_tag t('newsletter.subscribe') %>
<% end %>